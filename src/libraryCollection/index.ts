import { LibraryCollection } from '../types'

import { bunkyoLibrary } from './japan/tokyo/bunkyoLibrary'

export const libraryCollection: LibraryCollection = {
  'japan-tokyo-bunkyo': bunkyoLibrary,
}
